# m👀corne User Manual

> [!WARNING]
> This keyboard uses a Brazilian Portuguese (PT-BR) layout. Character keys and special symbols map accordingly. To use a different layout, adjust the keymap configuration.

This guide explains each layer and how to access them using keyboard combos. Keymap visuals are generated by [keymap-drawer](https://github.com/caksoylar/keymap-drawer).

## Table of Contents

- [How Layers Work](#how-layers-work)
- [Layer Access Quick Reference](#layer-access-quick-reference)
- [BASE Layer](#base-layer)
- [NAV Layer](#nav-layer-navigation--editing)
- [NUM Layer](#num-layer-numbers)
- [SYMBOLS Layer](#symbols-layer-shifted-symbols)
- [MOUSE Layer](#mouse-layer)
- [WM Layer](#wm-layer-window-management)
- [MEDIA Layer](#media-layer-media--bluetooth)
- [FUNC Layer](#func-layer-function-keys)

---

## How layers work

Your m👀corne uses keyboard combos (pressing two keys simultaneously) to activate layers. This keeps your hands near the home row instead of reaching for layer keys.

### Key position reference

The keyboard has 42 keys arranged in three rows per hand:

- **LM1–LM5**: Left hand, middle row (thumb keys are LM1–LM3)
- **RM1–RM5**: Right hand, middle row (thumb keys are RM1–RM3)
- **Top and bottom rows**: Referenced by hand and row

### Activation methods

Combo-based layers (press two keys together, then release):
- Release automatically after use or when you press other keys
- Useful for quick layer access without holding
- Support autolayer functionality (intelligent auto-deactivation for NAV, NUM, MOUSE)

Hold-based layers:
- MEDIA layer: Hold left `ESC` key to activate and use media controls, brightness, and Bluetooth

Combine layers with modifiers (Shift, Ctrl, Alt, Cmd) for advanced key combinations.

---

## Layer access quick reference

| Layer | Activation | Notes |
|-------|-----------|-------|
| BASE | Default | Colemak Mod-DH layout, optimized for ergonomics |
| NAV | Press LM3 + LM2 together | Cursor movement, page navigation, editing (autolayer) |
| NUM | Press LM3 + LM2 together (double-tap) | Number pad layout for fast typing (autolayer) |
| SYMBOLS | Press RM1 + RM2 together | Shifted symbols without chording Shift |
| MOUSE | Press LM1 + LM2 together | Mouse emulation and control (autolayer) |
| WM | Press LM3 + LM4 together | Window management shortcuts (macOS-friendly) |
| FUNC | Press RM3 + RM4 together | Function keys F1–F12 and shifted variants |
| MEDIA | Hold left `ESC` | Media controls, volume, brightness, Bluetooth |

---

## BASE layer

![my_keymap](../../keymap_img/keymap_base.svg)

This is your main typing layer using Colemak Mod-DH, an efficient, ergonomic letter arrangement.

**Home row modifiers:** Hold `A`, `R`, `S`, `T` on the left side for Ctrl, Alt, Shift, and GUI. Hold `N`, `E`, `I`, `O` on the right side for Shift, Ctrl, Alt, and GUI. This keeps you from leaving the home row to access modifier keys.

---

## NAV layer (navigation & editing)

![my_keymap](../../keymap_img/keymap_nav.svg)

Activate this layer by pressing **LM3 + LM2** together (left middle keys). The NAV layer uses autolayer technology, which means it automatically exits after a short idle period—you don't need to explicitly turn it off.

| Position | Function | Notes |
|----------|----------|-------|
| Home row | ← (Left), ↓ (Down), → (Right) | Arrow keys in HJKL-style positions |
| Above home row | Home, End, Page Up, Page Down | Navigate documents quickly |
| Home row modifiers | Ctrl, Alt, Shift | Combine with arrows to select text |
| Number keys | Quick access | For shortcuts and numbering |

**How autolayer works:** Hold the NAV layer while editing, then release. The layer stays active for another keystroke or two, then exits automatically. Use this to select words or move through text without re-activating the layer.

---

## NUM layer (numbers)

![my_keymap](../../keymap_img/keymap_num.svg)

Activate this layer by pressing **LM3 + LM2** together a second time (double-tap the same combo). The NUM layer uses autolayer, so it automatically exits after you finish typing numbers—no manual layer management needed.

| Position | Function |
|----------|----------|
| Top row | 7, 8, 9 |
| Middle row | 4, 5, 6 |
| Bottom row | 1, 2, 3 |
| Thumb (right) | 0 (zero) |
| Extra keys | . (dot) for decimals, modifiers available |

**Left-side modifiers are available** while typing numbers, so you can create combinations like `Shift+9` (opening parenthesis) without awkward hand positions.

---

## SYMBOLS layer (shifted symbols)

![my_keymap](../../keymap_img/keymap_sym.svg)

Activate this layer by pressing **RM1 + RM2** together (right middle keys). This is a one-time activation for quick symbol entry.

| Symbol | Use |
|--------|-----|
| `!`, `@`, `#`, `$`, `%` | Common symbols (top row) |
| `-`, `=`, `[`, `]`, `\` | Math and code brackets |
| `_`, `+`, `{`, `}`, `\|` | More code symbols |
| `(`, `)` | Parentheses for code |
| `^`, `&`, `*` | Math operators |

These are already shifted versions. Type them directly without holding Shift.

---

## MOUSE layer

![my_keymap](../../keymap_img/keymap_mouse.svg)

Activate this layer by pressing **LM1 + LM2** together (left hand, outer middle keys). The MOUSE layer uses autolayer technology for intuitive mouse control that automatically exits when you're done.

| Position | Function |
|----------|----------|
| Home row | Mouse movement: ← ↓ ↑ → |
| Below home row | Scroll wheel: Left, Down, Up, Right |
| Thumb keys (right) | Left click, Middle click, Right click |
| With modifiers | Hold Shift/Ctrl for slower/faster movement |

**How it works:** Use the mouse layer for precise control, then release. The layer automatically exits when you're done, making it feel like a natural extension of your typing.

---

## WM layer (window management)

![my_keymap](../../keymap_img/keymap_wm.svg)

Activate this layer by pressing **LM3 + LM4** together (left hand, lower-middle keys). This is a one-time activation for window snapping.

| Row | Function |
|-----|----------|
| Top row | Snap to screen corners (Cmd+7/8/9 and shifted variants) |
| Middle row | Snap to screen halves (Cmd+4/5/6 and shifted variants) |
| Bottom row | Center window and other positions (Cmd+1/2/3 and shifted variants) |

**Note:** Shortcuts use `Cmd+Number` combinations designed for macOS window management apps. Customize these key combinations if you use a different operating system.

---

## MEDIA layer (media & Bluetooth)

![my_keymap](../../keymap_img/keymap_med.svg)

Activate this layer by holding the left `ESC` key. Control media playback, volume, brightness, and Bluetooth connections.

| Position | Function |
|----------|----------|
| Top row | Brightness Down, Brightness Up |
| Middle row | Play/Pause, Previous track, Volume Down, Volume Up, Next track |
| Bottom row | Bluetooth profiles: BT 0, BT 1, BT 2, BT 3, BT 4 |
| Extra | BT Clear (bottom left), Mute |

### Switch Bluetooth profiles

1. Hold left `ESC` to activate the MEDIA layer.
2. Tap one of the BT buttons (0–4) to connect to that profile.
3. Hold Shift + BT button to clear that profile.

---

## FUNC layer (function keys)

![my_keymap](../../keymap_img/keymap_func.svg)

Activate this layer by pressing **RM3 + RM4** together (right hand, lower-middle keys). This is a one-time activation for function key access.

| Position | Function |
|----------|----------|
| Top row | F7, F8, F9, F10 |
| Middle row | F4, F5, F6, F11 |
| Bottom row | F1, F2, F3, F12 |
| Hold Shift | Shifted versions: Shift+F1, Shift+F2, and so on |

Use function keys for application shortcuts, system functions, or gaming macros.

---

## Getting started

Learn the layers progressively to build muscle memory:

1. **Start with BASE layer** — Practice Colemak Mod-DH and build muscle memory for the letters.
2. **Add NAV layer** — Learn the `LM3+LM2` combo and use it for editing and selection.
3. **Add NUM layer** — Learn number input with the same combo (double-tap). Use autolayer for fast typing.
4. **Explore SYMBOLS layer** — Add quick symbol access with `RM1+RM2`.
5. **Add MOUSE layer** — Control your mouse with `LM1+LM2` for trackpad-free computing.
6. **Master the rest** — Learn WM, FUNC, and MEDIA as needed for your workflow.

### Tips

- All layers preserve BASE layer's home row modifiers (Shift, Ctrl, Alt, Cmd).
- **Autolayer** (NAV, NUM, MOUSE) automatically deactivates, so you don't need to manage layer transitions manually.
- Combine combos with other keys for power combinations.
- Practice one layer at a time to build reliable muscle memory.
- Customize layer bindings in `config/corne.keymap` to fit your workflow.
